import { ComingSoon } from "@/components/construction";

export default function DealDetailPage() {
  return (
    <ComingSoon
      title="Deal detail"
      description="Tabs, scrubbable cashflow, interactive waterfall, and AI chat land in Phase 3. The full deal view is available in the legacy UI today."
      legacyHref="/legacy"
    />
  );
}

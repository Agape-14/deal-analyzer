import { ComingSoon } from "@/components/construction";

export default function PortfolioPage() {
  return (
    <ComingSoon
      title="Portfolio"
      description="Premium portfolio analytics (IRR, DPI, time-weighted returns, scrubbable cashflow charts) lands in Phase 4. The interactive dashboard is available in the legacy UI."
      legacyHref="/legacy"
    />
  );
}

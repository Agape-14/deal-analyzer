import { ComingSoon } from "@/components/construction";

export default function DevelopersPage() {
  return (
    <ComingSoon
      title="Developers"
      description="Sponsor profiles, track record, and per-developer analytics land in Phase 2. Full CRUD is available in the legacy UI."
      legacyHref="/legacy"
    />
  );
}
